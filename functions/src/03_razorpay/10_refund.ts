// import * as functions from "firebase-functions";
// import admin = require('firebase-admin');



// export const count_companies_increment_export = functions.region('asia-south1').runWith({ maxInstances: 2 }).firestore.document(`${}/{trigId}`).onCreate(async (change, context) => {
//     const db: FirebaseFirestore.Firestore = admin.firestore()
    
//     return;
// });